# AgentStage 功能清单

## 模块1：应用基础

| 功能编号 | 功能名称 | 功能描述 | 优先级 | 状态 | 备注 |
|---------|---------|---------|--------|------|------|
| APP-01 | Windows 安装包构建 | 提供 `.msi` 或 `.exe` 安装包，支持一键安装与卸载 | P1 | ✅ 已实现 | 最终交付物，开发阶段先用便携版调试 |
| APP-02 | 应用启动与初始化 | 启动应用后加载本地数据，展示主窗口 | P0 | ✅ 已实现 | 首次启动引导用户创建首个 角色 |
| APP-03 | 本地数据持久化 | 使用本地数据库（SQLite）存储会话、消息、角色配置、用户设置 | P0 | ✅ 已实现 | 强制便携模式，数据存在程序目录下 |
| APP-04 | 数据备份与恢复 | 支持导出/导入应用数据，防止数据丢失 | P1 | ✅ 已实现 | 角色/用户人设配置导出导入（AGT-20）已实现：导出为 `.agentstage` 专用配置包，包含基础配置、头像资源、长期记忆、关系、包内对象之间的好友关系 |
| APP-05 | 自动更新检测 | 应用启动时检测新版本，提示用户更新 | P2 | ⬜ 待实现 | 可配置是否自动检查 |
| APP-06 | 前后端分离日志 | 前端 console.log 通过 IPC 写入 `frontend.log`，后端写入 `backend.log`，均存储在 `data/logs/` | P1 | ✅ 已实现 | 便于调试和问题排查 |
| APP-07 | UI 美化与动画优化 | 优化聊天气泡、过渡动画、空状态、加载状态等视觉体验 | P2 | 🚧 部分实现 | 持续迭代，包含聊天气泡样式、头像展示、时间布局等 |
| APP-08 | 数据库初始化优化 | 新用户安装时直接创建最新版数据库结构，不走 V1→V7 逐版本迁移；升级用户仍按缺失版本逐个执行 | P1 | ⬜ 待实现 | 正式发布前统一处理，当前新用户需经历 V1→V7 完整迁移链 |
| APP-09 | UI 风格重构与插件主题 | 重构前端 UI 架构以支持插件化自定义模式，允许用户通过插件切换不同的视觉风格（主题、布局、组件样式等） | P1 | ⬜ 待实现 | 中高优先级：为后续社区主题生态做准备 |
| APP-09a | 主题系统图标扩展 | 当前主题仅支持 CSS 覆盖，无法替换 lucide-svelte 图标组件的形状。需设计图标注册/覆盖机制（如 CSS Mask、图标映射表或主题级组件覆盖），使主题可自定义图标风格 | P2 | ⬜ 待实现 | 依赖 APP-09，技术方案待评估：CSS Mask / 图标注册表 / 全局换库 |
| APP-09b | 主题系统布局自定义 | 当前主题无法改变组件位置或 DOM 结构。需扩展 theme.json 声明式布局配置（如面板显隐、导航位置、间距变量）或支持主题级组件覆盖 | P2 | ⬜ 待实现 | 依赖 APP-09，建议先走 CSS 变量 + 轻量 JSON 配置路线 |

---

## 模块2：角色 管理

| 功能编号 | 功能名称 | 功能描述 | 优先级 | 状态 | 备注 |
|---------|---------|---------|--------|------|------|
| AGT-01 | 创建 角色 | 用户创建一个新的 AI 角色，配置名称、头像、详细人设、简易人设、模型参数 | P0 | ✅ 已实现 | 模型必须支持 Function Calling，否则配置时报错 |
| AGT-02 | 编辑 角色 | 修改已创建 角色 的角色信息、模型配置 | P0 | ✅ 已实现 | 编辑后立即全局生效，所有现有会话的 Prompt 使用新人设 |
| AGT-03 | 删除 角色 | 删除不再需要的 角色 | P0 | ✅ 已实现 | 软删除，保留聊天记录 |
| AGT-04 | 角色 列表展示 | 侧边栏或独立页面展示所有已创建的 角色，支持搜索 | P0 | ✅ 已实现 | 卡片或列表视图 |
| AGT-05 | 模型 API 配置 | 为每个 角色 配置调用的 LLM 服务：选择模型、填写 API Key、Base URL、温度、Max Tokens 等 | P0 | ✅ 已实现 | 支持 OpenAI、Claude、Kimi、MiniMax 等兼容端点 |
| AGT-06 | API 连通性测试 | 配置完成后可发送测试请求验证 API 可用性，参考 cc-switch 实现方式 | P0 | ✅ 已实现 | 显示延迟和模型响应，需支持主流厂商（OpenAI/Claude/Kimi/MiniMax） |
| AGT-07 | 角色 头像与资料卡 | 支持上传/选择头像，查看 角色 详细资料 | P1 | 🚧 部分实现 | 头像上传已完成（含裁剪预览），资料卡 UI 待完善 |
| AGT-08 | 更换角色头像 | 在角色详情页支持点击头像上传新图片，自动保存到本地并更新 avatar_path | P2 | ✅ 已实现 | 支持常见图片格式（png/jpg/webp），自动压缩为 webp 存储 |
| AGT-09 | 保存成功提示 | 修改角色配置点击保存后，在上方弹出滑动提示“已保存” | P2 | ✅ 已实现 | 使用 Toast 提示 |
| AGT-10 | 双人设配置 | 每个 角色 独立维护「详细人设」（自身 System Prompt）和「简易人设」（给其他 角色 看的简介），均支持用户编辑 | P0 | ✅ 已实现 | 是 Prompt 拼接的核心数据来源 |
| AGT-11 | API Key 加密存储 | 角色配置的 API Key 使用 AES-256-GCM 加密后存入 SQLite，前端永不可见密文 | P0 | ✅ 已实现 | Rust 后端负责加密/解密 |
| AGT-13 | AI 辅助生成人设 | 基于用户输入的关键词或描述，调用 LLM 自动生成角色的详细人设和简易人设 | P1 | ✅ 已实现 | 减少用户手动编写人设的成本 |
| AGT-14 | 角色关系设定及维护 | 为每个角色配置对其他角色/用户人设的**单向关系描述**（主观看法，如"他是我的好朋友""不太喜欢他"），区别于客观简易人设。在Prompt【你认识的参与者】中注入。提供`update_relationship`工具让agent自主维护关系（200字限制，新旧文本匹配编辑）。角色详情页分标签页：【角色配置】/【关系设定】。用户删除人设后级联删除相关关系 | P1 | ✅ 已实现 | Migration V12 + agent_relationship repository + update_relationship tool + AgentRelationshipPanel UI |
| AGT-15 | 禁止创建同名角色 | 创建角色时若已存在同名非删除角色，阻断创建并提示用户 | P1 | ✅ 已实现 | 后端 `get_agent_by_name` 前置检查，前端按钮上方显示错误 |
| AGT-16 | 手动好友关系管理 | 在角色关系设定中支持手动添加/删除好友关系。添加时通过多选弹窗选择目标角色（支持将群友升级为好友），双向生效，主观描述保持为空。删除时仅可删除好友关系（群友不可删除），双向生效，删除后若双方仍在同群则降级为群友。同时修复群聊拉人时错误建立好友关系的Bug | P1 | ✅ 已实现 | 新增 `add_friendships` / `remove_friendship` 命令 + `AddRelationshipModal` / `ConfirmDeleteRelationshipModal` 弹窗 |
| AGT-17 | 初始化角色状态 | 提供一键初始化角色的功能，清除该角色的长期记忆、关系描述等动态状态，将其恢复到刚创建时的初始状态，保留角色配置本身 | P1 | ✅ 已实现 | `reset_agent_memory` 命令：清除 long_term_memory + 关系记忆（memory_text），保留角色配置和关系描述 |
| AGT-18 | 记忆系统数据模型与 UI 配置 | 为每个角色维护长期记忆（自身长文本）和对他人的记忆（按目标分组短文本）。角色配置页增加【记忆】标签页，包含启用开关、重置记忆、长期记忆编辑、对他人记忆编辑。记忆与关系复用同一套目标对象（表合并方案） | P0 | ✅ 已实现 | Migration V13 + agent/agent_relationship repo + update_agent_memory / reset_agent_memory 命令 + AgentMemoryPanel UI |
| **AGT-19** | **角色模型选择重构** | **全局设置中配置模型（可配置多个），每个角色从已配置模型中选择。链接测试功能移到全局配置。移除思考模式开关。角色保留 Temperature 配置，若角色配置了则覆盖模型本身的配置，若未配则使用所选模型自动配置的。配置模型时 Temperature 允许为空（非必填）。** | **P1** | **✅ 已实现** | **全局模型配置面板 + 角色模型选择器 + Migration V19 + LLM 层适配** |
| AGT-20 | 角色/用户人设配置导出导入 | 支持将一个或多个 AI 角色和用户人设导出为 `.agentstage` 专用配置包，并可从该文件导入。导出内容包含基础配置、头像资源、长期记忆、包内对象之间的好友关系、关系描述和对他人的记忆；未包含的角色关系将会被忽略并在导出前提示 | P1 | ✅ 已实现 | 导出到 `<exe_dir>/exports/bundles/`；导出通过弹窗选择角色/人设；导入预览可修改名称、为 AI 角色选择模型或一键应用模型到全部角色；导入重名时自动添加 `_1` / `_2` 后缀；未选择模型也允许导入，用户人设不自动激活 |

---

## 模块3：会话管理

| 功能编号 | 功能名称 | 功能描述 | 优先级 | 状态 | 备注 |
|---------|---------|---------|--------|------|------|
| SES-01 | 创建 1对1 会话 | 选择一个 角色 创建私聊会话 | P0 | ✅ 已实现 | 一个 角色 可拥有多个独立会话；创建后双方互为"好友"；从角色详情页提供"开始聊天"入口 |
| SES-02 | 创建群聊会话 | 选择多个 角色 创建群聊，支持命名和设置群头像 | P0 | ✅ 已实现 | 数据库层已支持，CreateGroupModal UI 已完成 |
| SES-03 | 会话列表展示 | 左侧边栏展示所有会话（1对1 + 群聊），按最近活动时间排序 | P0 | ✅ 已实现 | 显示未读消息数、最后一条消息预览 |
| SES-04 | 会话重置与历史管理 | **重置会话**：基于旧会话开启新 page（chat_pages 分页），成员不变，清空未读和冻结状态；**清除历史**：删除非当前 page 的所有历史消息和归档 page，保留当前 page；软删除保留聊天记录 | P2 | 🚧 部分实现 | 重置会话、清除历史、软删除均已实现；置顶/归档 UI 未实现 |
| SES-05 | 会话配置面板 | 为私聊和群聊提供统一的配置面板，支持修改名称/头像、设置自动消息条数限制、群聊成员增删查看 | P0 | ✅ 已实现 | 整合禁言、消息上限、历史提示条数、重置会话、解散群聊 |
| SES-06 | 会话搜索 | 按会话名称、参与者名称搜索会话 | P1 | ⬜ 待实现 | 实时搜索 |
| SES-07 | 群聊禁言开关 | 每个群聊独立提供禁言开关，开启后该群聊不再自动触发任何 角色 调用 | P0 | ✅ 已实现 | 纳入会话配置面板（SES-05），UI 已完成 |
| SES-09 | 解散群聊 | 群主/用户可解散群聊，清空群成员并标记群聊为已解散，聊天记录可选择保留或清除 | P2 | ✅ 已实现 | 区别于软删除，解散后群聊不可恢复 |
| SES-10 | 清除会话历史 | 右键菜单清除会话历史：删除所有非当前 page 的历史消息和归档 page，保留当前 page 消息；清除后从历史列表消失，Reset 后重新出现 | P1 | ✅ 已实现 | `clear_session_history` 命令 + `list_history_sessions` 按 `chat_pages` 数量过滤 |
| SES-11 | 已删除角色会话处理 | 角色被删除后，其私聊会话在会话列表中隐藏；历史列表中显示"(已删除)"标记；已删除角色的私聊输入框禁用 | P1 | ✅ 已实现 | `SessionParticipant.is_deleted` + 前端列表过滤 + 输入禁用 + 标记显示 |
| SES-12 | 会话重置时保留历史参与者快照 | 重置会话时，历史记录保留当时的参与者姓名、头像和简易人设，确保回看历史消息和历史模式续聊时使用当时的参与者展示信息 | P2 | ✅ 已实现 | V22 `chat_page_participants` 参与者快照 + 历史模式消息渲染/Prompt 使用快照；关系标签、关系描述和记忆文本按当前状态实时查询，不做历史快照 |

---

## 模块4：聊天核心

| 功能编号 | 功能名称 | 功能描述 | 优先级 | 状态 | 备注 |
|---------|---------|---------|--------|------|------|
| CHAT-01 | 用户发送消息 | 用户在输入框输入文本并发送，消息展示在聊天区域 | P0 | ✅ 已实现 | 支持 Enter 发送，Shift+Enter 换行 |
| CHAT-02 | 角色 回复消息 | 角色 接收上下文后调用 LLM API 生成回复 | P0 | ✅ 已实现 | 私聊显示"正在输入中"提示，回复完成后展示；群聊不需要此提示 |
| CHAT-03 | 消息气泡展示 | 按发送者区分展示消息气泡，支持时间戳、头像 | P0 | ✅ 已实现 | 类似微信/QQ 的聊天气泡 |
| CHAT-04 | 聊天记录滚动与加载 | 聊天区域支持滚动查看历史消息，支持跳转到最新消息 | P0 | ✅ 已实现 | 基本滚动 + chat_pages 分页 + History 模式完整 UI 已实现 |
| CHAT-05 | 群聊 角色 自动触发 | 群聊禁言关闭后，任何角色发送消息会自动触发群聊中其它 角色 的调用；群聊消息达到上限后强制停止 | P0 | ✅ 已实现 | 调度器已完整支持群聊多角色触发链 |
| CHAT-06 | 用户介入 角色 对话 | 用户在 角色 自动对话过程中可随时发送消息参与 | P0 | ✅ 已实现 | 用户消息进入上下文，正常触发其它 角色 |
| CHAT-07 | 消息复制与重新生成 | 支持复制消息内容，对 角色 消息支持"重新生成" | P1 | ⬜ 待实现 | 重新生成替换原消息或插入新消息 |
| CHAT-08 | 消息引用/回复 | 支持引用某条消息进行针对性回复 | P1 | ⬜ 待实现 | 长按或右键菜单 |
| CHAT-09 | 聊天上下文管理 | 支持查看当前会话的系统提示词拼接逻辑，支持清空上下文 | P1 | ⬜ 待实现 | 技术用户可查看原始 Prompt |
| CHAT-10 | send_message Tool | 为 角色 提供 Function Calling 工具 `send_message`，参数含目标类型、目标 ID、内容，调用成功后不返回结果给 角色 | P0 | ✅ 已实现 | 不支持 Tool Calling 的模型无法配置 |
| CHAT-11 | 私聊自动触发 | 私聊中任何角色（用户或 角色）发送消息后，触发接收方 角色 的调用；私聊消息达到上限后强制停止 | P0 | ✅ 已实现 | 两个 角色 私聊时受全局最小间隔控制，避免高频循环 |
| CHAT-12 | 可见消息历史维护 | 为每个 角色 独立维护其可见的全部消息历史，按「私聊对象」和「群聊」分组 | P0 | ✅ 已实现 | Prompt 拼接时按此结构注入 |
| CHAT-13 | 全局最小触发间隔 | 每个 角色 拥有全局最小触发间隔（默认 30s），间隔内积压的所有新消息一次性组装调用 | P0 | ✅ 已实现 | 适用于所有私聊和群聊触发场景 |
| CHAT-14 | 消息上限配置 | 私聊和群聊分别可配置 角色 消息条数上限（不含玩家消息），达到上限后强制停止触发；默认开启，支持关闭 | P0 | ✅ 已实现 | 私聊默认 20 条，群聊默认 30 条 |
| CHAT-15 | 乐观消息展示 | 用户发送消息后，消息气泡应立即出现在聊天框中（无需等待后端响应） | P0 | ✅ 已实现 | 避免用户误以为发送失败 |
| CHAT-16 | LLM Provider 兼容性 | OpenAI-compatible Provider 需兼容 Minimax 等国内厂商（至少包含一个 user message） | P0 | ✅ 已实现 | Minimax 要求请求体中不能只有 system message，已自动处理 |
| CHAT-17 | LLM 错误提示优化 | LLM 调用失败时不使用 alert 弹窗，改用可关闭的 Toast；取消重试机制避免多次报错 | P1 | ✅ 已实现 | Toast 通知替代 alert，错误保留便于调试 |
| CHAT-18 | 重试机制控制 | 移除 LLM 的 3 次重试逻辑，失败一次即停止并提示用户 | P1 | ✅ 已实现 | 减少 API 调用成本和用户干扰 |
| CHAT-19 | SSE 服务端事件推送 | 调度器通过 Server-Sent Events 向前端推送 `agent_typing`、`new_message`、`agent_completed`、`agent_error` | P0 | ✅ 已实现 | Tauri v2 Event 机制 |
| CHAT-20 | 前端 Toast 通知 | 全局 Toast 组件，支持成功/错误/信息提示；系统提示自动消失（带倒计时进度条），报错信息需手动关闭 | P1 | ✅ 已实现 | 替代原生 alert；系统提示 10s 自动消失，报错不自动消失 |
| CHAT-21 | 会话去重 | 创建私聊时自动检测是否已存在同角色会话，避免重复创建 | P0 | ✅ 已实现 | 返回已有会话而非新建 |
| CHAT-22 | 消息发送者信息展示 | 后端 JOIN `agents` 表获取 `sender_name` 和 `sender_avatar`，群聊中正确显示发言角色名称和头像 | P0 | ✅ 已实现 | 避免 N+1 查询 |
| CHAT-23 | 扩展 Tool Calling 能力 | 为角色提供除 `send_message` 外的更多工具，如文件读取、网页搜索、本地命令执行等，实现本地助手能力。人设自生成(AGT-13)已预留 websearch 接口，待后续实现 | P2 | ⬜ 待实现 | 需评估安全性和权限控制 |
| CHAT-24 | 聊天窗口滚动位置控制 | 进入聊天窗口时自动滚动到最新消息位置；批量加载消息时自动滚动到底部；单条新消息到达时，仅在用户已在底部时自动滚动 | P2 | ✅ 已实现 | 新增 `isNearBottom` 判断，避免打断用户浏览历史 |
| CHAT-25 | 消息上限重置按钮 | 消息达到上限后，在输入框上方显示"重置限制"按钮；点击后计数归0，解除冻结，自动触发有未读消息的角色回复（遵循CD） | P1 | ✅ 已实现 | 重置后无需用户发新消息即可恢复对话 |
| CHAT-27 | Session Inbox 架构重构 | 用按 session×agent 的未读消息层替代 pending_queue，支持上限冻结持久化；上限到达后新消息不再进入未读队列，重置后恢复 | P0 | ✅ 已实现 | Migration V6 + scheduler 重构 + frozen_states + agent_unread_queue |
| CHAT-28 | Prompt 模板常量化 | 将 System Prompt、Layer 标题、引导语等所有硬编码提示词提取到独立文件（`prompt_templates.rs`），方便后续修改 | P1 | ✅ 已实现 | PromptAssembler 已去掉 Layer 5 改为单层时序排序，全部模板在 prompt_templates.rs |
| CHAT-26 | 模型调用详细日志 | 记录每次 LLM 调用的完整输入（Prompt）和输出（Response），便于后续调试和问题排查 | P1 | ✅ 已实现 | PromptAssembler 已记录完整 prompt 到 backend.log；Response 日志待完善 |
| CHAT-29 | 私聊参与者对称重构 | 将 private_sessions 从单边 agent_id 重构为对称 participant_1/2_type/id，支持未来 Agent-Agent 私聊 | P1 | ✅ 已实现 | Migration V7 + 全部 Repository 层适配 |
| CHAT-30 | 提示词视角化名称 | PromptAssembler 中私聊名称从角色视角显示为"和{对方}的私聊"；用户人设运行时替换 | P1 | ✅ 已实现 | get_user_persona() + get_session_name() 视角化改造；Layer 3 用户条目 relation 改为"好友" |
| CHAT-31 | 未读消息计数与会话隔离 | messageStore 按会话隔离存储；未读计数-badge；SessionList 点击清除未读 | P1 | ✅ 已实现 | Map<string, Message[]> 重构 + incrementUnreadCount / clearUnreadCount |
| CHAT-32 | 历史会话模式 | 支持查看历史 chat_page 的聊天记录，独立 HistoryPromptAssembler  bypass Scheduler | P1 | ✅ 已实现 | HistorySessionList + ChatView mode="history" + send_history_message 命令 |
| CHAT-33 | Agent-to-Agent 私聊 | 角色可自主通过 `start_private_chat` Tool 开启与其他角色的私聊并发送第一句话；用户可旁观所有角色间私聊；会话列表和标题显示双头像；消息固定站位；用户不可输入 | P0 | ✅ 已实现 | `start_private_chat` Tool + symmetric private session + 双头像 UI + 禁用输入 + 自动互加好友 |
| CHAT-34 | 历史消息不污染当前预览 | 历史会话模式中发送的消息不更新 `sessions.last_message_preview`，避免当前 page 为空时预览显示历史消息 | P1 | ✅ 已实现 | `send_history_message` 不更新 preview；前端历史模式不移除 `updateSessionPreview` |
| CHAT-35 | 发送消息后切会话防覆盖 | 用户在会话 A 发送消息后切到会话 B，A 的 AI 回复到达时不应覆盖 B 的聊天内容；后台静默更新并在会话列表显示未读 | P0 | ✅ 已实现 | `handleSend` 发送成功后检查当前选中会话，已切走则跳过 `loadMessages`；`new_message` 防御检查生效 |
| CHAT-36 | 记忆功能 — 长期任务维护 | 为每个角色维护跨会话的长期记忆，支持记录和追踪长期任务目标、关键事实和关系演变 | P0 | ✅ 已实现 | 拆分为 AGT-18（数据模型）、CHAT-39-ext（Prompt注入）、CHAT-37-ext（update_memory工具）、CHAT-37（重置总结）、CHAT-40（溢出总结）、CHAT-41（定时任务）、CHAT-42（主动会话） |
| CHAT-37 | 重置会话时AI总结记忆 | 每次重置会话（私聊/群聊）时，调用相关角色的AI总结当前page中的可用信息，更新该角色的长期记忆 | P1 | ✅ 已实现 | Scheduler spawn_session_summary + SUMMARY_SYSTEM_PROMPT |
| CHAT-38 | 禁言逻辑优化 | 优化群聊禁言的实现逻辑，确保禁言状态下所有角色触发机制被完全阻断，包括边缘场景（如定时任务、系统通知触发等） | P0 | ✅ 已实现 | trigger_agent_inner 过滤 muted sessions；trigger_special Timer 入口检查禁言；execute_send_message 阻止发送到禁言会话 |
| CHAT-39 | 重构提示词拼接方案 | 将当前PromptAssembler的拼接逻辑重构为标准的 System Prompt + User Message 结构，替代现有的多层混合拼接，提升兼容性和可维护性 | P1 | ✅ 已实现 | CHAT-39-ext: 复用 list_relationships_by_observer + 结构化参与者格式 + 记忆/关系分层注入 |
| CHAT-40 | 超长消息批量处理 | 当会话消息超出阈值时，自动触发AI批量总结，更新角色长期记忆 | P1 | ✅ 已实现 | overflow_summary_threshold + last_overflow_summary_index + 消息插入时触发 |
| CHAT-41 | 定时任务工具 | 角色可通过 create_timer/delete_timer 工具设定定时任务（单次/循环），到时间后无视CD触发一次调用 | P1 | ✅ 已实现 | Migration V15 + scheduled_tasks表 + LLM工具 + 前端定时任务面板 |
| CHAT-42 | 主动会话机制 | 角色在空闲时主动发起话题，可配置时间区间 + 全局安静时段，触发时由角色自选会话 | P1 | ✅ 已实现 | proactive_enabled + proactive_timers内存计时 + 安静时段 + 前端配置 |
| CHAT-43 | 表情包系统 | 支持自定义表情包（批量导入图片、按包管理），AI 根据表情名称理解语境并通过 `<sticker>包名_表情名</sticker>` 标签发送表情；前端输入框、消息气泡渲染展示表情图片 | P1 | ✅ 已实现 | StickerPack 表 + CRUD/导入导出(.agentsticker) + Prompt 注入 sticker 上下文 + RichTextarea 内联渲染 + StickerPickerPanel 底部面板 + MessageBubble 渲染 + 会话列表预览显示 [表情] |
| CHAT-44 | 消息朗读（TTS） | 支持用户点击消息气泡朗读消息内容；支持按角色配置不同语音（音色/语速）；接入本地或云端 TTS 服务 | P1 | ⬜ 待实现 | 后端提供 TTS 命令，前端提供朗读按钮和语音设置面板；大需求 |

---

## 模块4.5：VITS 语音与 TTS

| 功能编号 | 功能名称 | 功能描述 | 优先级 | 状态 | 备注 |
|---------|---------|---------|--------|------|------|
| VOICE-01 | VITS 运行时检测 | 检测 `data/vits_runtime/` 是否已放置；扫描 `data/vits_models/` 下的 .pth 模型 | P0 | ⬜ 待实现 | 未放置运行时禁用语音功能 |
| VOICE-02 | 角色语音配置 | 在角色编辑页新增【语音】Tab，绑定 VITS 模型、设置目标语言、情感参数、语速 | P0 | ⬜ 待实现 | 模型目录由用户手动放置 |
| VOICE-03 | 消息语音朗读 | 在配置了语音模型的角色消息旁增加喇叭按钮，点击后播放对应语音 | P0 | ⬜ 待实现 | 后端生成 WAV 文件后播放 |
| VOICE-04 | 语音生成时机 | 支持三种模式：自动生成并播放 / 自动生成不播放 / 点击后生成并播放 | P1 | ⬜ 待实现 | 按角色配置，默认自动生成不播放 |
| VOICE-05 | VITS 参数调节 | 支持 emotion、length_scale、speaker 等模型参数调节 | P1 | ⬜ 待实现 | 根据模型 config 动态展示 |
| VOICE-06 | 语音缓存管理 | 所有生成的语音保存到 `data/vits_cache/`，支持查看和清理 | P1 | ⬜ 待实现 | 按会话/角色分组 |
| VOICE-07 | 翻译开关与提示 | 当文本语言与目标语言不一致时自动翻译，并提示额外 LLM 开销 | P0 | ⬜ 待实现 | 默认开启 |
| VOICE-08 | 翻译模型选择 | 可选择翻译使用的 LLM 模型，默认使用角色已配置模型 | P1 | ⬜ 待实现 | 复用现有 model_config |
| VOICE-09 | 翻译判断工具 | 后端提供独立 LLM 工具，返回 need_translate 和 translated_text 两个参数 | P0 | ⬜ 待实现 | 翻译时注入人设/关系/记忆 |
| VOICE-10 | TTS 翻译开销统计 | 翻译调用在模型用量统计中单独分类（trigger_type="tts_translate"），支持按角色筛选 | P1 | ⬜ 待实现 | 复用 llm_usage_records，不新增字段 |

---

## 模块5：用户角色（P2 优先级）

| 功能编号 | 功能名称 | 功能描述 | 优先级 | 状态 | 备注 |
|---------|---------|---------|--------|------|------|
| USR-01 | 用户人设配置 | 用户可创建并管理自己的人设（名称 + 简介 + 头像），支持多套人设切换，默认人设为代码常量 | P1 | ✅ 已实现 | ProfileView + UserPersonaConfig UI 已完成；后端表 + 命令 + Prompt 注入均已完成 |
| USR-02 | 会话中选择用户身份 | 在每个会话中，用户选择以"普通用户"或"某人设"参与 | P2 | ⬜ 待实现 | 默认普通用户 |
| USR-03 | 人设同步给 角色 | 当用户以某人设参与会话时，将该人设信息注入该会话所有 角色 的 System Prompt | P2 | ✅ 已实现 | PromptAssembler 已支持运行时查询 user_personas 注入 System Prompt |
| USR-04 | 用户头像与昵称 | 配置默认用户头像、昵称和简易人设，在所有会话中作为基础身份展示 | P1 | ✅ 已实现 | 默认头像存 `app_settings.default_avatar_path`；昵称通过 `user_personas.name` 获取；群聊成员列表和消息气泡已正确显示用户头像和昵称 |
| USR-05 | 切换人设提醒 | 用户切换人设时弹出提醒：切换人设可能导致已有对话历史与当前人设存在差异，造成角色理解混乱，询问是否一键重置所有聊天 | P0 | ✅ 已实现 | 高优先级：防止人设切换后角色认知不一致 |

---

## 模块6：系统设置

| 功能编号 | 功能名称 | 功能描述 | 优先级 | 状态 | 备注 |
|---------|---------|---------|--------|------|------|
| SET-01 | 通用设置 | 设置语言、主题（明亮/暗黑）、字体大小 | P1 | ⬜ 待实现 | 默认跟随系统 |
| SET-02 | 快捷键配置 | 查看和自定义应用快捷键 | P2 | ⬜ 待实现 | 如 Ctrl+N 新建会话 |
| SET-03 | 关于页面 | 展示应用版本、开源协议、检查更新 | P2 | ⬜ 待实现 | 标准关于对话框 |
| SET-04 | 全局触发间隔设置 | 配置所有 角色 的全局最小触发间隔（默认 30s） | P1 | ✅ 已实现 | 可调节 5-300 秒，SettingsPanel UI 已完成 |
| SET-05 | 消息上限设置 | 配置私聊和群聊的 角色 消息上限数值，以及是否启用上限 | P1 | ✅ 已实现 | 会话级配置面板已完成，私聊默认 10 条，群聊默认 30 条 |
| SET-06 | 系统提示词模板调整 | 允许高级用户查看和修改全局 System Prompt 模板（如角色认知、工具说明等），影响所有角色的 Prompt 拼接 | P1 | ✅ 已实现 | 所有模板已提取到 `llm/prompt_templates.rs` 常量文件，便于集中修改 |
| **MON-01** | **模型用量监测** | **追踪 LLM 调用 Token 消耗，支持按模型/角色/会话/用途多维度统计，含趋势图表和明细查询** | **P1** | **✅ 已实现** | **Migration V21 + 11 个 Query 方法 + 11 个 Tauri Commands + 6 Tab 前端页面** |

---

## 优先级定义

| 优先级 | 含义 | 标识 |
|-------|------|------|
| P0 | MVP 必须，核心功能 — 缺少则产品不可用 | 🔴 |
| P1 | 重要增强，首版应有 — 缺少影响体验 | 🟡 |
| P2 | 体验优化，可延后 | 🟢 |

---

## 完整性检查清单

### 角色 相关
- [x] 创建 角色 → 有编辑/删除
- [x] 配置模型 API → 强制要求支持 Tool Calling
- [x] 角色 人设 → 支持详细人设 + 简易人设（双人设）
- [x] 角色 列表 → 有搜索
- [x] API Key 加密 → AES-256-GCM，前端不可见密文（AGT-11）
- [x] API 连通性测试 → 已实现（AGT-06，全局模型配置 `test_model_config_connection`）
- [x] 角色头像上传 → 已实现（AGT-07~08）
- [x] 角色关系设定及维护 → 已实现（AGT-14）

### 会话相关
- [x] 创建会话 → 1对1 和群聊均已完成
- [x] 会话管理 → 软删除已实现；置顶/归档 UI 待完善
- [x] 群聊核心 → 创建、成员列表、自动触发、消息上限均已完成
- [x] 群聊禁言开关 → 已实现（SES-07，纳入配置面板）
- [ ] 会话搜索 → 待实现（SES-06）
- [x] 会话配置面板 → 已实现（SES-05，整合禁言/消息限制/成员管理/重置/解散）

### 聊天相关
- [x] 发送消息 → 有 角色 回复
- [x] 角色 聊天 → 自然触发 + 禁言控制 + 全局最小间隔 + 消息上限
- [x] 角色 发送 → 提供 `send_message` Tool，强制要求 Function Calling
- [x] 历史消息 → 按 角色 视角独立维护，私聊/群聊分组
- [x] Prompt 拼接 → 明确顺序：系统 → 自身人设 → 好友/群友简易人设 → 历史消息 → 最新消息
- [x] 防循环 → 消息条数上限 + 全局时间间隔双重保护
- [x] 防死锁 → 启动时自动重置 is_triggering + stale 检测（5 分钟超时）
- [x] 乐观更新 → 用户消息立即显示，失败自动移除（CHAT-15）
- [x] 消息发送者信息 → 后端 JOIN 查询，群聊正确显示头像和名称（CHAT-22）
- [x] SSE 事件推送 → typing / new_message / completed / error（CHAT-19）
- [x] Toast 通知 → 替代 alert；系统提示带倒计时自动消失，报错手动关闭（CHAT-20）
- [x] 会话去重 → 同角色私聊自动返回已有会话（CHAT-21）
- [x] 聊天记录分页加载 → 已实现（CHAT-04）
- [x] Agent-to-Agent 私聊 → `start_private_chat` Tool + 双头像 + 固定站位 + 禁用输入（CHAT-33）
- [x] 历史消息不污染预览 → `send_history_message` 不更新 preview（CHAT-34）
- [ ] 消息复制/重新生成/引用 → 待实现（CHAT-07~08）
- [ ] 上下文查看 → 待实现（CHAT-09）
- [ ] 扩展 Tool Calling → 待实现（CHAT-23）
- [x] 聊天窗口滚动位置控制 → 仅在底部时自动滚动（CHAT-24）
- [x] 消息上限重置按钮 → 已实现，乐观更新立即消失（CHAT-25）
- [x] 模型调用详细日志 → 已实现（CHAT-26）
- [x] Session Inbox 架构重构 → 已实现（CHAT-27）
- [x] Prompt 模板常量化 → 已实现（CHAT-28）
- [x] 私聊参与者对称重构 → 已实现（CHAT-29）
- [x] 提示词视角化名称 → 已实现（CHAT-30）
- [x] 未读消息计数与会话隔离 → 已实现（CHAT-31）
- [x] 历史会话模式 → 已实现（CHAT-32）

### 用户角色
- [x] 用户人设配置（含头像/昵称/简易人设） → 已实现（USR-01）
- [x] 用户默认头像与昵称 → 已实现（USR-04）
- [ ] 人设使用 → 待实现（USR-02）
- [x] 人设同步 → 已实现（USR-03）

### 系统设置
- [x] 全局触发间隔 → 已实现（SET-04）
- [x] 消息上限 → 已实现（SET-05，会话级配置面板）
- [ ] 主题/外观 → 待实现（SET-01）
- [ ] 快捷键 → 待实现（SET-02）
- [ ] 关于页面 → 待实现（SET-03）
- [ ] 系统提示词模板调整 → 待实现（SET-06）

### 模型用量监测
- [x] 模型用量监测 → 已实现（MON-01）
  - 按模型统计（Expandable Row 下钻到角色）
  - 按角色统计（下拉框选择 + 模型分布）
  - 按会话统计（概览/按角色/按模型/角色×矩阵 4 子 Tab）
  - 按用途统计（双饼图：调用次数 + Token 消耗）
  - 明细查询（分页 + 4 维度筛选）
  - 时间范围：今日/近7天/近30天/本月/全部

### 待完善需求
- [ ] **群聊最少人数限制**：当前后端限制至少 2 个角色、前端也限制至少 2 个角色，加上用户后实际为 3 人。需明确并统一前后端的群聊最少人数校验逻辑，确保群聊始终满足最少成员要求（含用户），避免后续出现 2 人群聊等边界情况。

### 已知问题（待修复）
- [x] **Bug-重置后预览未清空**：已修复 — `resetSession` 成功后手动清空 `last_message_preview`（Batch 1）
- [x] **Bug-正在输入不消失**：已修复 — `onMount` 闭包改用 `$state` 变量 `currentAgentId` 做比较（Batch 1）
- [x] **优化-正在输入缺头像**：已修复 — typing indicator 添加角色头像（Batch 1）
- [x] **Bug-消息发送人显示未知**：已修复 — `get_message_by_id` 改用 JOIN agents 表获取 `sender_name`（Agent-Agent 私聊）
- [x] **Bug-历史消息污染当前预览**：已修复 — `send_history_message` 不再更新 `last_message_preview`（CHAT-34）
- [x] **Bug-头像加载失败（dev 模式）**：已修复 — `convertFileSrc` 在 dev 模式下不可用，改用 Vite `@fs/` 服务加载本地文件
- [x] **Bug-删除角色后列表未刷新**：已修复 — `AgentDetail` 删除后调用 `agentStore.loadAgents()`；`AgentList` 改用 `agentStore.agents` 代替本地状态
- [x] **Bug-清除历史后会话重新出现**：已修复 — `historyStore` 改调 `list_history_sessions`，按 `chat_pages` 数量 > 1 过滤；`clear_session_history` 保留 `last_message_preview`
- [x] **Bug-发送消息后切会话内容被覆盖**：已修复 — `handleSend` 发送成功后检查当前选中会话，已切走则跳过 `loadMessages`，防止 `currentSessionId` 被错误重置（CHAT-35）

---

## 实现顺序建议（已更新）

1. **阶段一：角色 配置 + UI（已完成）**
   - 角色 的创建、编辑、双人设配置、模型 API 配置、强制 Tool Calling 检测、角色 列表 UI
   - 应用基础框架和本地数据存储（强制便携模式）

2. **阶段二：用户与 角色 的 1对1 对话（已完成）**
   - 私聊会话创建、消息收发、好友关系建立
   - Prompt 拼接逻辑（含好友/群友筛选）
   - `send_message` Tool 接入
   - 全局最小触发间隔、私聊消息上限
   - **Bug 修复**：UTF-8 安全截断、is_triggering 死锁防护

3. **阶段三：群聊核心功能（已完成）**
   - 群聊创建 UI（CreateGroupModal）、成员列表侧边栏
   - 群聊中的多 角色 自动触发逻辑（调度器已支持）
   - 群聊消息上限、简易人设同步、可见消息历史维护
   - **待完善**：会话配置面板（禁言/消息限制/成员管理）、群聊解散

4. **阶段四：Agent-to-Agent 私聊（已完成）**
   - `start_private_chat` Tool：角色自主开启与其他角色的私聊
   - Symmetric private session 支持 Agent-Agent
   - 用户视角旁观所有角色间私聊（双头像、固定站位、禁用输入）
   - 自动互加好友、消息上限、历史提示条数、重置会话均支持

5. **V1.1（已实现 / 待排期）**
   - ✅ AI 辅助生成人设（AGT-13）
   - ✅ 角色关系设定及维护（AGT-14）
   - ✅ 用户角色配置（USR-01）
   - ✅ 模型用量监测（MON-01）
    - 待排期：会话管理增强（置顶、归档、搜索、续开）
    - ✅ API 连通性测试（AGT-06）
    - 待排期：用户默认头像与昵称（USR-04）、系统提示词模板调整（SET-06）
    - 待排期：消息重新生成/引用（CHAT-07~08）、上下文查看（CHAT-09）
    - ✅ 安装包构建（APP-01）
    - 待排期：数据备份恢复（APP-04）

6. **V2.0（待排期）**
   - 用户角色扮演系统完整版（USR-01~04）
   - 扩展 Tool Calling（文件读取、网页搜索等本地助手能力）
   - 图片/文件消息支持
   - UI 美化与动画优化（APP-07）
   - VITS 语音合成机制（VOICE-01~10）：VITS 模型目录管理、角色语音配置、消息朗读、生成时机、语音缓存、自动翻译、翻译开销统计

---

*文档版本：V2.2*  
*编写日期：2026-05-17*  
*更新说明：
- 新增 CHAT-35（发送消息后切会话防覆盖）、SES-10（清除会话历史）、SES-11（已删除角色会话处理）、AGT-15（禁止创建同名角色）
- AGT-07 状态更新为"部分实现"（头像上传已完成），AGT-08 状态更新为"已实现"
- SES-04 描述更新：重置会话、清除历史、软删除均已实现；置顶/归档 UI 未实现
- 更新已知问题列表：头像加载、删除角色列表刷新、清除历史重复出现、切会话内容覆盖等 bug 已修复
- PRD 同步更新：Function Calling 强制检测标记为 MVP 暂不实现；加密存储描述更新为 AES-256-GCM 本地加密；SES-04 描述同步实际实现
- AGT-14 扩展为"角色关系设定及维护"：明确单向关系描述、update_relationship工具、Prompt注入、标签页UI、用户人设级联删除等设计
- USR-01 状态更新为"已实现"：ProfileView + UserPersonaConfig 前端UI已完成
- **AGT-13 状态更新为"已实现"**：AI 辅助生成人设 — 两步多轮对话（fill_character_fields 工具调用 + XML 标签解析），支持新建角色和已有角色双模式
- **AGT-14 状态更新为"已实现"**：角色关系设定及维护 — Migration V12 + 完整前后端实现
- **USR-03 状态更新为"已实现"**：PromptAssembler 已支持运行时查询 user_personas 注入 System Prompt
- V1.1 实现顺序建议更新：AGT-13/14、USR-01 标记为已完成*
